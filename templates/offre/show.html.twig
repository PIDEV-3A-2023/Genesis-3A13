<!DOCTYPE html>

<title>Offre</title>

{% block body %}
    <h1>Offre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdOffre</th>
                <td>{{ offre.idOffre }}</td>
            </tr>
            <tr>
                <th>PourcentageSolde</th>
                <td>{{ offre.pourcentageSolde }}</td>
            </tr>
            <tr>
                <th>PrixSoldé</th>
                <td>{{ offre.prixSoldé }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_offre_index') }}">back to list</a>

    <a href="{{ path('app_offre_edit', {'idOffre': offre.idOffre}) }}">edit</a>

    {{ include('offre/_delete_form.html.twig') }}
{% endblock %}
