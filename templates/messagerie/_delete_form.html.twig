<form method="post" action="{{ path('app_messagerie_delete', {'idMessagerie': messagerie.idMessagerie}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ messagerie.idMessagerie) }}">
    <button class="btn">Delete</button>
</form>
