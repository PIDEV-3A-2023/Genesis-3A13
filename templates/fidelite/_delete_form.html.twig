<form method="post" action="{{ path('app_fidelite_delete', {'idFidelite': fidelite.idFidelite}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fidelite.idFidelite) }}">
    <button class="btn">Delete</button>
</form>
