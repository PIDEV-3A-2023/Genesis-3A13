<!DOCTYPE html>

<title>Panierlivre index</title>

{% block body %}
    <h1>Panierlivre index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdPanierLivre</th>
                <th>Quantite</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for panierlivre in panierlivres %}
            <tr>
                <td>{{ panierlivre.idPanierLivre }}</td>
                <td>{{ panierlivre.quantite }}</td>
                <td>
                    <a href="{{ path('app_panierlivre_show', {'idPanierLivre': panierlivre.idPanierLivre}) }}">show</a>
                    <a href="{{ path('app_panierlivre_edit', {'idPanierLivre': panierlivre.idPanierLivre}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_panierlivre_new') }}">Create new</a>
{% endblock %}
