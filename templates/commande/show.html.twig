{% extends 'baseBack.html.twig' %}

{% block title %}Détails Commande
{% endblock %}

{% block body %}
 <a href="{{ path('app_commande_index') }}"><i class="fas fa-arrow-left"></i></a>
    <h1>Détails Commande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCommande</th>
                <td>{{ commande.idCommande }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ commande.status }}</td>
            </tr>
            <tr>
                <th>Mode</th>
                <td>{{ commande.mode }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ commande.etat }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ commande.montant }}</td>
            </tr>
        </tbody>
    </table>

   
    <a href="{{ path('app_commande_edit', {'idCommande': commande.idCommande}) }}" class="btn btn-warning d-inline-flex align-items-center"> <i class="fas fa-edit" style="padding-right:3px"></i>modifier</a>

    {{ include('commande/_delete_form.html.twig') }}
{% endblock %}
