{% extends 'baseBack.html.twig' %}

{% block title %}Détails Evenement
{% endblock %}

{% block body %}
  	<div class="d-flex justify-content-between w-100 flex-wrap">
		<div class="mb-3 mb-lg-0">
			<h1>
				<span class="sidebar-icon">

					<img src="{{asset('/Photos/evenements/' ~ evenement.image)}}" class="icon icon-s me-2" fill="currentColor" viewbox="0 0 20 20" class="card-img-top rounded-circle border-white" alt="Logout">

				</span>Evenement : {{ evenement.nom }}</h1>
                
		</div>
	</div>

    <table class="table">
        <tbody>
            <tr>
                <th class="text-primary fw-bold">Nom</th>
                <td>{{ evenement.nom }}</td>
            </tr>
            <tr>
                <th class="text-primary fw-bold">Date</th>
                <td>{{ evenement.date ? evenement.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th class="text-primary fw-bold">Heure</th>
                <td>{{ evenement.heure ? evenement.heure|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th class="text-primary fw-bold">Lieu</th>
                <td>{{ evenement.lieu }}</td>
            </tr>
            <tr>
                <th class="text-primary fw-bold">Description</th>
                <td>{{ evenement.description }}</td>
            </tr>
            <tr>
                <th class="text-primary fw-bold">NbTicket</th>
                <td>{{ evenement.nbTicket }}</td>
            </tr>
        </tbody>
    </table>

  
<div class="row">
		<div class="col-md-2">
    <a href="{{ path('app_evenement_edit', {'idEvenement': evenement.idEvenement}) }}"  class="btn btn-warning d-inline-flex align-items-center"><i class="fas fa-edit" style="padding-right:3px"></i>Modifier</a>
</div>
		<div class="col-md-2">
    {{ include('evenement/_delete_form.html.twig') }}
    </div>
	</div>
{% endblock %}
